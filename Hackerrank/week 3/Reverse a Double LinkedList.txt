 DoublyLinkedListNode *curr = llist, *temp = NULL;
    
    while (curr != NULL) {
        temp = curr->prev;
        curr->prev = curr->next;
        curr->next = temp;
   
        if (curr->prev == NULL)
            return curr;
        curr = curr->prev;
    }
    return NULL;
}